# stackvm.defn


declareext(name, extern, attributes)
    .external(name, attributes)
    ## Emit a declaration for an external global
    ## If name in builtin table, use its signature,
    ##  else use type from attributes.

declare(name, attributes)
    .alloca(name, attributes)
    ## Allocate storage for name, with type from attributes

